name: PR Check

on:
  pull_request:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Install pytest
        run: pip install pytest

      - name: Run tests
        run: python -m pytest tests/test_sanity.py